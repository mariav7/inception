[server]

[mysqld]

user                    = mysql
pid-file                = /run/mysqld/mysqld.pid
socket                  = /run/mysqld/mysqld.sock
port                    = 3306
basedir                 = /usr
datadir                 = /var/lib/mysql
skip-external-locking

# Bind address to allow external connections
bind-address            = 0.0.0.0

[embedded]

[mariadb]

[mariadb-10.5.21]
